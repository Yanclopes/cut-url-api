# API
PORT=3000

# PostgreSQL
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=cut_url
DB_HOST=postgres
DB_PORT=5432

# Prisma
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}
